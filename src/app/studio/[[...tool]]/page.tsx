import { StudioClient } from "@/components/StudioClient";

export const metadata = { title: "Sanity Studio · Noah Neu", robots: { index: false, follow: false } };

export default function StudioPage() {
  return <StudioClient />;
}
